# دليل البدء السريع - Quick Start Guide

## 🚀 نظام الحجز جاهز للاستخدام!

تم إنشاء نظام حجز متكامل للخدمات. هذا الدليل يساعدك على البدء بسرعة.

---

## ✅ ما تم إنجازه

- ✅ 6 API endpoints جديدة
- ✅ جدول payments في قاعدة البيانات
- ✅ نظام حساب الأسعار مع الخصومات
- ✅ دعم الكوبونات
- ✅ نظام دفع مرن (يدعم أي بوابة دفع)
- ✅ إدارة الحجوزات (عرض، تفاصيل، إلغاء)

---

## 📋 الـ API Endpoints

### 1️⃣ حساب السعر (اختياري)
```
POST /api/v1/customers/services/calculate_price
```

### 2️⃣ بدء الحجز
```
POST /api/v1/customers/services/initiate_booking
```

### 3️⃣ تأكيد الحجز (بعد الدفع)
```
POST /api/v1/customers/services/confirm_booking
```

### 4️⃣ عرض جميع الحجوزات
```
GET /api/v1/customers/bookings
```

### 5️⃣ تفاصيل حجز معين
```
GET /api/v1/customers/bookings/{id}
```

### 6️⃣ إلغاء حجز
```
POST /api/v1/customers/bookings/{id}/cancel
```

---

## 🔥 مثال سريع

### الخطوة 1: بدء الحجز
```bash
curl -X POST http://localhost/api/v1/customers/services/initiate_booking \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "service_id": 1,
    "customer_address_id": 5,
    "order_date": "2026-02-25",
    "order_time": "14:30",
    "coupon_code": "DISCOUNT20"
  }'
```

### الخطوة 2: معالجة الدفع
```javascript
// في التطبيق، توجيه المستخدم لبوابة الدفع
// والحصول على transaction_id
```

### الخطوة 3: تأكيد الحجز
```bash
curl -X POST http://localhost/api/v1/customers/services/confirm_booking \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "service_id": 1,
    "customer_address_id": 5,
    "order_date": "2026-02-25",
    "order_time": "14:30",
    "coupon_code": "DISCOUNT20",
    "payment_method": "paymob",
    "transaction_id": "TXN_123456789"
  }'
```

---

## 📱 سير العمل في التطبيق

```
صفحة الخدمة
    ↓
[زر: احجز الآن]
    ↓
صفحة الحجز
├── اختيار التاريخ والوقت
├── اختيار العنوان
├── إدخال كود الكوبون (اختياري)
└── [زر: متابعة للدفع]
    ↓
استدعاء: initiateBooking()
    ↓
صفحة الدفع
├── عرض السعر النهائي
└── [زر: ادفع الآن]
    ↓
توجيه لبوابة الدفع (Paymob/Tabby)
    ↓
[نجاح الدفع]
    ↓
استدعاء: confirmBooking()
    ↓
[رسالة نجاح]
    ↓
صفحة حجوزاتي
```

---

## 🔐 المتطلبات

1. **تسجيل دخول العميل**:
   - جميع الـ endpoints تتطلب `Authorization: Bearer {token}`

2. **عنوان مسجل**:
   - يجب أن يكون للعميل عنوان واحد على الأقل
   - استخدام `customer_address_id` في الحجز

3. **خدمة نشطة**:
   - الخدمة يجب أن تكون `status = true`

---

## 💡 نصائح مهمة

### 1. استخدام الكوبونات
```json
{
  "coupon_code": "DISCOUNT20"  // اختياري
}
```
- إذا كان الكوبون صحيح، يتم تطبيق الخصم تلقائياً
- إذا كان غير صحيح، يتم إرجاع خطأ

### 2. التاريخ والوقت
```json
{
  "order_date": "2026-02-25",  // يجب أن يكون اليوم أو بعده
  "order_time": "14:30"        // صيغة 24 ساعة
}
```

### 3. طريقة الدفع
```json
{
  "payment_method": "paymob",     // أي string
  "transaction_id": "TXN_123",    // من بوابة الدفع
  "payment_response": "{...}"     // JSON response (اختياري)
}
```

---

## 📊 حالات الحجز

| الحالة | الوصف |
|--------|-------|
| `pending` | في انتظار التأكيد |
| `confirmed` | تم التأكيد |
| `completed` | تم الإنجاز |
| `cancelled` | ملغي |

---

## 🎯 الخطوات التالية

### 1. تطوير واجهة المستخدم
- صفحة الحجز
- صفحة الدفع
- صفحة حجوزاتي

### 2. التكامل مع بوابة الدفع
اختر واحدة:
- **Paymob**: [https://paymob.com](https://paymob.com)
- **Tabby**: [https://tabby.ai](https://tabby.ai)
- أو أي بوابة أخرى

### 3. إضافة Webhooks
لاستقبال إشعارات بوابة الدفع تلقائياً

### 4. الإشعارات
- إشعار عند تأكيد الحجز
- تذكير قبل الموعد
- إشعار عند الإلغاء

---

## 📚 المستندات الكاملة

- **BOOKING_SYSTEM.md**: شرح كامل للنظام
- **BOOKING_API_EXAMPLES.md**: أمثلة تفصيلية لكل endpoint
- **SUMMARY_AR.md**: ملخص شامل بالعربي

---

## ❓ أسئلة شائعة

### س: هل يمكن الحجز بدون كوبون؟
✅ نعم، الكوبون اختياري تماماً

### س: هل يمكن استخدام أكثر من طريقة دفع؟
✅ نعم، النظام مرن ويدعم أي طريقة دفع

### س: ماذا يحدث إذا فشل الدفع؟
⚠️ لا يتم إنشاء الحجز. يجب استدعاء `confirmBooking()` فقط بعد نجاح الدفع

### س: هل يمكن إلغاء حجز مكتمل؟
❌ لا، يمكن إلغاء الحجوزات في حالة `pending` أو `confirmed` فقط

### س: كيف أعرف السعر النهائي قبل الدفع؟
✅ استخدم `calculatePrice()` أو `initiateBooking()` - كلاهما يعيد السعر النهائي

---

## 🎉 جاهز للاستخدام!

النظام جاهز تماماً. يمكنك الآن:
1. اختبار الـ APIs باستخدام Postman
2. تطوير واجهة المستخدم
3. التكامل مع بوابة الدفع

**حظاً موفقاً! 🚀**
